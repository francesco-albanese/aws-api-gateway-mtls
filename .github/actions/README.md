# Github Actions

This directory contains reusable GitHub Actions which encapsulate a common setup used in multiple github workflows.

## configure-aws-env

Selects AWS role by environment, configures credentials via OIDC, optionally setup Terraform.

## setup-python-uv

Installs Python + uv package manager.
